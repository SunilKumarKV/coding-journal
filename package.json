{
  "name": "coding-journal-cli",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "validate": "node scripts/validate.js",
    "build": "node scripts/build.js",
    "sync": "npm run validate && npm run build",
    "test": "node --test",
    "cli": "node ./bin/cj.js"
  },
  "bin": {
    "cj": "./bin/cj.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "commander": "^14.0.3",
    "dotenv": "^17.4.2"
  }
}
